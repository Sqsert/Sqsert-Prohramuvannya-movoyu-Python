N = int(input("Введіть ціле число N (1 < N < 9): "))
if 1 < N < 9:
  for i in range(N):
        for j in range(i + 1, N + 1):
            print(j, end=" ")
        print()
else:
    print("Помилка: число N повинно бути в межах від 2 до 8")
